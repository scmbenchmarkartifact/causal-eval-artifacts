# Benchmark Files

This directory contains the frozen official causal SCM benchmark runtime YAML
files used by the anonymous NeurIPS 2026 submission.

The files are large because they contain benchmark records and embedded result
records used for offline report reproduction. For code-hosting services with
size limits, upload this directory to a dedicated dataset host and keep the
small files in `data/samples/` in the code repository.
